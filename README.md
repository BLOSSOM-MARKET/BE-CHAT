config 폴더 > default.js 생성 후
```
module.exports = {
    mysql:         {   
        host :'localhost',   
        port : 3306,   
        user : user이름,   
        password : 패스워드,   
        database: 'bm'  
    },
    port: 3001,
    client: "localhost:3000"
}
```
